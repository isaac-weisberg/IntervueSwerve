// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "IntervueSwerve",
    products: [
        .executable(
            name: "IntervueSwerve",
            targets: [ "IntervueSwerve" ]),
    ],
    dependencies: [
        
    ],
    targets: [
        .target(
            name: "IntervueSwerve",
            dependencies: [  ])
    ]
)
